<Project>
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>

  <!-- MinVer drives the package version from git tags (MinVerTagPrefix "v" -> tags like v0.1.0).
       Pre-tag builds produce a height-based 0.0.0-alpha prerelease; the first release tag is v0.1.0.
       PrivateAssets=All keeps MinVer itself out of consumers' dependency graphs. -->
  <ItemGroup>
    <PackageReference Include="MinVer" Version="6.*" PrivateAssets="All" />
  </ItemGroup>

  <PropertyGroup>
    <MinVerTagPrefix>v</MinVerTagPrefix>
  </PropertyGroup>

  <PropertyGroup Condition="'$(IsPackable)' != 'false'">
    <Authors>PipelineZ contributors</Authors>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <RepositoryUrl>https://github.com/PipelineZ/pz-connector-snowflake</RepositoryUrl>
    <PackageProjectUrl>https://pipelinez.dev</PackageProjectUrl>
    <!-- "pipelinez-connector" is the ratified discovery tag for pz connector packages: it is the
         one query a project author runs on nuget.org to find installable connectors, so it must
         appear here and must NOT appear on non-connector packages. -->
    <PackageTags>pipelinez-connector;pipelinez;pz;snowflake;etl;elt;data-pipeline;arrow</PackageTags>
    <PackageReadmeFile>README.md</PackageReadmeFile>
  </PropertyGroup>

  <ItemGroup Condition="'$(IsPackable)' != 'false'">
    <None Include="$(MSBuildThisFileDirectory)README.md" Pack="true" PackagePath="\" />
  </ItemGroup>
</Project>
